#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int solve(ll n) {
    string s = to_string(n);
    vector<ll> pows;
    pows.push_back(2);

    
}

int main() {
    ios::sync_with_stdio(0);
	cin.tie(0);

    ll t, n;
    cin >> t;

    while (t--) {
        cin >> n;
        cout << solve(n) << endl;
    }
}