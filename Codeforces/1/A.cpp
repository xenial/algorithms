#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int solve(int n, int m, int a) {
    
}

int main() {
    ios::sync_with_stdio(0);
	cin.tie(0);
    
    int n, m, a;
    cin >> n >> m >> a;
    cout << solve(n, m, a);
}